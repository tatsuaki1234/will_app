<div class="show-article">
  <div class="show-article-box">
        <%# <div class="content_post" style="background-image: url(<%= article.image %>
        <div class="article-show-nickname">
          <h1 class="show-article-user">投稿者名：　<%= @article.user.nickname %></h1>
        </div>
        <div class="article-show-title">
          <p class="show-article-title">タイトル：　<%= @article.title %></p>
        </div>
        <div class="article-show-content">
          <%= @article.content %>
        </div>
        <%# </div> %>
  </div>
</div>

<div class="show-btns">
    <% if user_signed_in? %>
      <% if current_user.id == @article.user_id %>
          <div class="ppoi">
            <%= link_to '商品の編集', edit_article_path(@article.id), method: :get, class: "article-edit-btn" %>
          </div>
          <div class='or-text'>or</div>
          <div class="ppoi">
            <%= link_to '削除', article_path(@article.id), method: :delete, class:'article-destroy-btn' %>
          </div>
      <% end %>
    <% end %>
</div>
